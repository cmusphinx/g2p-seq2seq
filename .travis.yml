dist: trusty
language: python
python:
 - "2.7"
 - "3.5"
install:
 - pip install --upgrade pip
 - pip --version
 - pip install tensorflow==1.0.0
 - python setup.py build
script:
 - python setup.py test

